class Account {
    /**
     * Возвращает баланс кошелька по публичному ключу
     * 
     * @param {String} publicKey публичный адрес участника сети
     * 
     * @returns {Number} balance
     */
    static getBalance(publicKey) {
        //алгоритм получения баланса
    }
}

module.exports = Account;
